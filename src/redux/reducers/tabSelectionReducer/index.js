
const initialState = {

}

const tabSelectionReducer = (state=initialState, action) => {
    switch(action.type) {
        default:
            return {
                ...state
            }
    }
};

export default tabSelectionReducer;